{
  "name": "thcs-chaodau",
  "version": "1.0.0",
  "private": true,
  "description": "Portal trường THCS Dương Minh Châu",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute duongminhchau --file=./schema.sql --remote",
    "db:local": "wrangler d1 execute duongminhchau --file=./schema.sql --local",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
